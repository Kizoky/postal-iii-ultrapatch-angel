Changelogs between hotfixes will appear here
CHANGELOG_ANGEL_v1.3.0.txt will also get extended

## v0.4

- Fixed Cats and Motorhead duplicating everytime Player went through a transition zone
- Fixed Motorhead being glued onto the Player's hands
- Cats (in correct order) and Motorhead now remember their P3S attributes and other data
- Fixed some children of ea_transition P3S entities not travelling through
- Fixed Cats that are part of Player's weapon not transferring over
- Fixed a crash inside Postal3Script when loading between maps (after finishing a mission)



## v0.3

- Game no longer allows to load Vanilla/pre-Ultrapatch save files (this was done because people kept loading old saves which caused crashes)
- Fixed `p3_ultrapatch_precache_voice` precaching garbage string and non-existent files
- Added `p3_ultrapatch_precache_voice_log`, logs successfully precached files as well as non-existent ones for debugging
- Fixed a crash on the main menu if some script files are missing (like the difficulty one) or when the host is completely offline and cannot fetch updates
- p3increaseprecache.dll: Fixed sound precache over 8192 actually working and not turning everything into grabbing random sounds (thanks Grizzle!)
- AngelScript: Disallow value assign for ref types (to increase stability and prevent crashes)
- AngelScript: Added hook `NPC::OnFSMStart`, fires after NPC's AI has been ran for over 3 seconds, perfect place to check for relationships or targets on "spawn" which normally you can't do because of P3S
- Script: Fixed Glow system not obeying the instant cvar
- Script: Added a generic Glow system fallback for maps that aren't supported by default, it checks for Player and escort haters, as well as checks for escorts and gives them glow color
- Fixed Fart gun missing conscious min and max values
- Fixed Taser taking ammo when reloading not taking the reloading duration's into account
- Fixed Player forever reloading the Taser if holstered while being fired and then deployed
- Fixed bugs when Taser's max clip size is not -1 (recommended to set it to 1, higher values are not tested and it might be janky)
- Picking up other Tasers while max clip size is not -1 will now give +1 clip size always
- AngelScript: Fixed `int GetEffects()` in `CBaseEntity@` using the wrong function
- `p3_ultrapatch_act_debug` now also shows the cycling time for active layers
- Added `host_thread_mode 0` to autoexec cfg
- Optimized various vmts for materials (f_nude_nazi, salad prop)
- Fixed pizza inventory item pickup for PA, PWAC, and roam_police maps
- Added Achievement Debugger (Can be accessed via `Insert key -> Windows -> Achievement Debugger`)
- Failed achievements now display a console message
- Fixed a crash when Player saves the game with a Tank's attachment already destroyed then attempts to load same save file
- Fixed game crashing when Karma changes path (from Good to Evil or vice versa)
- Fixed game crashing when Bees were present on the map (When NPCs were trying to avoid active Bees)
- NPCs no longer avoid arrested NPCs in their pathfinding
- NPCs no longer avoid Bees in their pathfinding (this also solves issues like NPCs wiggling from left to right sometimes)
- Updated M249 FPS model
- Fixed M60's texture's alpha channel being horizontally shifted which made the weapon look off
- Fixed M60's bipod blades not being transparent
- Fixed the M60's reload soundscript using the M249 reload sound
- Added a thirdperson reload animation to the M60
- Added HL2 jeep (ch_createjeep)
- Fixed CW not giving the fireaxe (Vanilla oversight)
- Fixed vehicles (like the HL2 jeep) still glowing after entering them
- Fixed Player getting ragdolled while inside a vehicle (non-Segways only)
- Player is now forced to exit the Segway before ragdolling
- Added Cuda model from Alpha leak
- Postal3Script: Fixed P3S pointer for weapons becoming invalid after save/load or transition
- Fixed game crashing if p3 save icon panel is missing and trying to save
- Added flame sound for Spray alternative attack Weapon_Spray.Flame
- Added smoke trail for Grenades when thrown
- Fixed hold pos being screwed for Cat Weapon
- Postal3Script: Added P3S event `OnSmellsLikeCatnip` when Catnip spray is applied onto Human NPCs
- Postal3Script: P3S event `OnApplyCatnip` no longer fires when the Cat is takeable (is high)
- Cats will now go after animals that had been catnip applied onto
- Fixed not being able to dismember corpses with the Fireaxe
- Fireaxe will no longer smash heads
- Fireaxe will now always gib
- Ported over Flashbang implementation from CR, this uses the model `models/Weapons/flashbang/flashbang.mdl`, NPCs are stunned for 5 seconds
- Fixed Gasoline and Sprays dropping onto the floor if the Player has full ammo but no owned weapon of those types when trying to pick them up
- Fixed Gasoline ammo not appearing on the UI properly after Holster/Deploy
- Fixed the hold pos for the Grenade being borked if stayed near wall when holstered
- Hammer no longer gibs
- Hammer always smashes head
- Hammer now causes screen shake when destroying heads
- Postal3Script: Added P3S area events for Laserpen for these unused modes: `WhistleMotorhead`; `WhistleChamp` (`Whistle` remains unchanged when changed to All type)
- Can no longer attach cats onto M249
- Machete now always collides with NPCs while attacking
- Added `OnThrown` p3s event for meta weapons when they are being thrown
- Fixed *mostly* the Bulky weapon, should be less broken now
- Fixed being able to suck in invisible objects with the Shopvac
- Added `origin` param for `shopvac_` events (ModEvents.res) (x, y ,z)
- Shovel no longer gibs heads (don't confuse it with smashing which is different)
- Nerfed Weemote's burst loss rate (from 20 to 40)
- Nerfed Spray flame distance from 50 to 35
- AngelScript: Fixed AngelScript VGUI huds absolutely not functioning
- AngelScript: Fixed crashes when trying to use scriptified VGUI huds
- AngelScript: Fixed `ExamplePanel` always getting created
- Added p3_vehicle_wheelchair (WIP, spawnable via p3_ultrapatch_spawn_wheelchair)
- AngelScript: Fixed an issue where VGUI panels would be just shown as a black rectangle (reloads the hud scheme on first tick)
- AngelScript: VGUI panels can now embed HudLayout data via `string LAYOUT_<param>` variables, they must absolutely be a string
- AngelScript: Added `vgui::HealthPanel`
- AngelScript: Added implicit (auto) and manual casting between vgui panels
- AngelScript: Renamed `CBaseEntity::GetClassName` to `CBaseEntity::GetClassname`
- AngelScript: Casting between `CHudElement` and `vgui::Panel` is now possible
- `wj_c_angelscript_recompile` is now functional and acts like the alternative cvar (there's no difference between these two on client)
- AngelScript: VGUI panels now get deleted when recompiling
- AngelScript: Registered `CBaseEntity`, `CBasePlayer`, `CBaseCombatCharacter`, `CBaseCombatWeapon`, `CP3Player`, `CP3Weapon`, `CFSMItemProp` and `CWJProp` for client-side AngelScript along with (most) of their methods
- AngelScript: Added `CBasePlayer@ GetLocalPlayer()` global function
- Disabled the HL2 hint system (the one that's trying to give Half-Life 2 hints to the Player, not actual HUD element)
- Updated FPS bare hands anims
- Updated FPS sniper rifle anims
- Corrected an error in compound prop doc
- Fixed the game crashing due to AngelScript's broken string implementation
- Added Catharsis Reborn style Stats system, stats get properly saved onto save file, only the server knows about all the Players stats, on client it is entirely local only
- Added experimental new loading screen which overrides Vanilla loading screens from gameui.dll (Not enabled by default, you can use `-newloading` to test it out, uses UI/loadingscreen.res as scheme)
- Nav generation now also takes Player clips into account (previously it only took NPC clips into account)
- AngelScript: Added a way to get and clear input keys (gHud, input)
- AngelScript: Registered some client-side `engine` functions
- AngelScript: Fixed cached functions not purging on recompile
- AngelScript: Fixed broken string formatting, it is now advised to always use `FormatString`
- AngelScript: Fixed float formatting (%.f, %.2f, %.4f, etc...)
- AngelScript: Registered `InButtons_t` enum
- Fixed weapons crashing after going transit (was reproducable going from pdb -> pw)
- Fixed Weapon janitor crashing the game sometimes
- Fixed a crash when NPCs use melee weapons
- Added new loading screen (can be used with `-newloading` as currently it is experimental)



## v0.2

- Fixed a crash when trying to look up difficulties
- Fixed a crash when a P3S pointer is null and it tries to fire an event inside itself
- Fixed Tear Gas crashing the game after transition or loading a save file
- Updated Grizzle's sound expansion plugin (which also fixes buggy sounds)
- Fixed a crash inside studiorender.dll (by Grizzle)
- Forced `UTIL_SetModel` to precache model and not crash the game (like on GMOD)
- Added update checker both for git commits and releases
- Fixed inventory items failing to restore on transition/or crashing the game
- Fixed `cl_playermodel` not defaulting to coat on new game/spawn (which caused funny stuff like randomly becoming krotchy on a freeroam map)
- Fixed a crash when Dogs get hurt and they are in a squad
- Fixed animals creating no knocked out ragdolls and becoming braindead
- Fixed `ent_info player` crashing the game
- Fixed arresting softlocking the player if they held a limb before (now it drops everything before arresting)
- Fixed vaulting over softlocking the player while they were spamming jumping and kicking
- Fixed being able to jump while arresting
- Added non-dev console message when `select_weapon` fails
- Updated FPS Autoshotgun
- FPS Animations and Models are now merged with Ultrapatch (manually enabling the addon is no longer required)
- Fixed URL panel closing other ImGui panels


## v0.1

- Initial release