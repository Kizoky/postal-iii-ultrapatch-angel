// Code will only compile for server-side
#ifdef SERVER
// This number is looked for when loading save file, you can up this number everytime you make heavy changes to the codebase
// If before and after numbers don't match, the Serializer will automatically abort when attempting to restore values
// NOTE: This absolutely must be an integer!
const int AS_MOD_VERSION = 0;

#include "server/engine.as"
#include "server/npc.as"
#include "server/player.as"
#include "server/glow_helper.as"
#include "server/difficulty.as"
#include "server/inv_items.as"
#include "server/interactable.as"

#include "server/entities/teargas.as"


#include "server/glow_helper.as"

#include "server/misc/background.as"
#include "server/misc/trafficlight.as"
#include "server/misc/utils_npc.as"

//#include "example.as"

#include "shared/team.as"

#include "server/utils.as"
#include "server/utils_enum.as"

#endif

// Code will only compile for client-side
#ifdef CLIENT

#include "client/engine.as"
#include "shared/team.as"




#endif