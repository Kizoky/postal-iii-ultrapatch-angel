// This file is used to create inventory items, Angel by default has no inventory items! (Use this file as a guide)
// Make sure to have an unique namespace, since it will be used for stacking items!
// Mods/Addons can have inventory items via `p3/addons/<addon_name>/scripts/<addon_name>_inventory.txt`

// Inventory items must use 'wj_prop_fsmitem' prop in maps or scripts
// This prop has a keyfield called 'TransformItem', for the pizza example above this should be 'angel::pizza'
// do note that you must always use a namespace!

// for scripters, create the entity as normal, but before spawning it call the function "TransformItem"
// inside AngelScript this would be for the example above:
// pointer.TransformItem("angel::pizza");
// 
// If you do not want to use this, you can modify the properties just before spawning it, check out our AngelScript Docs for this.

"inventory_item"
{
	// Unique namespace, shouldn't conflict with other mods
	"angel"
	{
		// Name of the inventory item, this will be used to create inventory items on spawn or at a later time
		// This should be referred to angel::pizza in maps and AngelScript (both server and client)
		"pizza"
		{
			"displayName"	"Pizza"								// Name of the inventory item
			"behavior"		"bh_angelitem_pizza" 				// Postal3Script behavior
			"faction"		"Items"				 				// Postal3Script faction
			"manner"		"itmFood"			 				// Postal3Script manner
			"initState"		"st_init"			 				// Postal3Script init state of bh_angelitem_pizza
			"startState"	"st_start"			 				// Postal3Script start state of bh_angelitem_pizza
			
			"stacking"		"1"					 				// (Default: 1)
																// This item can be stacked, won't occupy multiple inventory slots when picked up multiple times
			
			"maxStacking"	"0"					 				// (Default: 0)
																// The maximum number of stacking, Player won't be able to pick up any more of this type of item
																// and won't occupy another slot (Set to 0 to allow unlimited amount of stacks)
												 
			"canBeDropped"	"1"					 				// (Default: 1)
																// Whether the item could be dropped by the Player or not
																// scripters can still force it to be dropped or deleted
			
			"touch" 		"1"									// (Default: 1)
																// Whether Players are able to pick up the item by touching it or not (Postal 2-style)
																// This can be used to setup special pick up rules (inside P3S) like making the Player pick it up in their hands first
																
			"blink" 		"1"									// (Default: 1)
																// Item will pulsate/blink like in Postal 2 or like the weapons do in Ultrapatch already
																
			"scripting" 	"1"									// (Default: 1)
																// Item will have working Postal3Script when it is stored in a Player or an NPC's inventory
																// Some events (like OnDrop) and functions might stop working properly if disabled
																
			"model"			"models/small_things/pizza_01.mdl"	// The inventory item's new model, this can be left blank to use the model provided by the mapper or scripter
			"icon"			"hud/pizza"							// Used by client, the icon for the inventory item to display
			
			// Properties of the inventory item to keep track of uniqueness of each stacked item
			// When picked up the game checks if such p3s attribute exists, if yes then it'll save this property
			// The game will remember and apply these attributes accordingly 
			// (for example Pizza 4 was pissed on, but Player picked up 3 more pizzas making it 7)
			// (Pizza 5-7 will never have the pissy attribute, but the fourth Pizza will do)
			"properties"
			{
				"attributes"
				{
					// name of the P3S attribute	// leave it blank as it's unused
					"pissy"							""
				}
				
				// Preserve the delay, repeated (+next delay), and AngelScript flag
				// example: "Timer tMyTimer,1,repeated" will become "OnTimer_tMyTimer"
				"timers"
				{
					// name of the timer			// leave it blank as it's unused
					//"OnTimer_xxx"					""
				}
			}
		}
		
		"stats"
		{
			"displayName"	"Stats"								// Name of the inventory item
			"behavior"		"bh_angelitem_stats" 				// Postal3Script behavior
			"faction"		"Items"				 				// Postal3Script faction
			"manner"		"itmGeneric"			 			// Postal3Script manner
			"initState"		"st_init"			 				// Postal3Script init state of bh_angelitem_pizza
			"startState"	"st_start"			 				// Postal3Script start state of bh_angelitem_pizza
			
			"stacking"		"0"					 				// (Default: 1)
																// This item can be stacked, won't occupy multiple inventory slots when picked up multiple times
			
			"maxStacking"	"1"					 				// (Default: 0)
																// The maximum number of stacking, Player won't be able to pick up any more of this type of item
																// and won't occupy another slot (Set to 0 to allow unlimited amount of stacks)
												 
			"canBeDropped"	"0"					 				// (Default: 1)
																// Whether the item could be dropped by the Player or not
																// scripters can still force it to be dropped or deleted
			
			"touch" 		"1"									// (Default: 1)
																// Whether Players are able to pick up the item by touching it or not (Postal 2-style)
																// This can be used to setup special pick up rules (inside P3S) like making the Player pick it up in their hands first
																
			"blink" 		"1"									// (Default: 1)
																// Item will pulsate/blink like in Postal 2 or like the weapons do in Ultrapatch already
																
			"scripting" 	"1"									// (Default: 1)
																// Item will have working Postal3Script when it is stored in a Player or an NPC's inventory
																// Some events (like OnDrop) and functions might stop working properly if disabled
																
			"model"			"models/small_things/pizza_01.mdl"	// The inventory item's new model, this can be left blank to use the model provided by the mapper or scripter
			"icon"			"hud/inventory/stats"				// Used by client, the icon for the inventory item to display
			
			// Properties of the inventory item to keep track of uniqueness of each stacked item
			// When picked up the game checks if such p3s attribute exists, if yes then it'll save this property
			// The game will remember and apply these attributes accordingly 
			// (for example Pizza 4 was pissed on, but Player picked up 3 more pizzas making it 7)
			// (Pizza 5-7 will never have the pissy attribute, but the fourth Pizza will do)
			"properties"
			{
				"attributes"
				{
					// name of the P3S attribute	// leave it blank as it's unused
					//"pissy"							""
				}
				
				// Preserve the delay, repeated (+next delay), and AngelScript flag
				// example: "Timer tMyTimer,1,repeated" will become "OnTimer_tMyTimer"
				"timers"
				{
					// name of the timer			// leave it blank as it's unused
					//"OnTimer_xxx"					""
				}
			}
		}
	}
}