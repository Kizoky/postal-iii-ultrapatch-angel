https://www.moddb.com/mods/postal-iii-ultrapatch/downloads/postal-iii-ultrapatchangel-v130
==============
v1.3.0: (Note: skipped version v1.2.0 on purpose, consider Ultrapatch+Angel v1.3.0 to be the main version of Ultrapatch from now on)

## Highlights  
- New features and fixes for the AngelScript implementation
- New features and fixes for Postal3Script
- Fixed buggy Door destruction
- You can now shoot or kick while jumping
- UltraPatch Angel is now compatible with all copies of Postal 3
- Experimental Demo playback fix (Sound is working, there are some issues with the camera/prediction)
- Added new weapons (Colt, Autoshotgun, M249, Tear Gas)
- Fixed several crashes and memory corruptions
- Improved and fixed the Addon System
- Made PhysX completely non-functional (can't fix it, please don't ask in the future about it)
- Added FPS camera (p3_ultrapatch_cam_fps_toggle)
- Added `-novid`, skips intro videos
- Intro and credits can now be skipped
- `p3_cam_shake` console command no longer disables quake/explosion effects
- Added `-disablevideo` to completely disable (cutscene) videos in-game
- Made `func_monitor` and other such entities work again
- "Fixed" not being able to shoot while in cover mode sometimes
- Reimplemented cut Inventory system (very similar to Catharsis Reborn's implementation)
- Multiplayer fixes and new features like Teams and Spectator mode
- Players and NPCs can now open doors just by simply touching them (no need for trigger brushes anymore)
- Increased engine model (1024->4096) and sound (8192->16384) precache limits (thanks Grizzle!)
- New UI combined with GameUI2 and ImGui
- Fixed weapons and some other props not fading out in the same distance as NPCs do
- Heli can now start smoking and it will intensify the less health it has (feature from CR)
- Heli now always uses its Postal3Script target as shooting target
- NPCs while on Alert or Combat state will no longer move away from the Player
- Motorhead will now stick to where the Laser actually is
- Fixed save file bloat coming due to level transitions
- Added about 41+ new P3S functions from CR, you can find some examples from here: https://github.com/WhackJobInt/postal3scriptdocs/commit/8e79d29f859cdc46f4a183a7b14e5f9611a2d545
- Dogs can now attack the Player
- Added Left 4 Dead/Alien Swarm instructor system
- Added in-game Addon/Mod Manager

* AngelScript: Fixed possible crash on server-side
* Fixed Door's parent entities not disappearing when destroyed
* AngelScript: global function "Teleport" now uses the same spot check like Multiplayer spawn spot checking
* Added a bunch of null checks for the Fluids Manager to prevent crashes (Gasoline)
* Fixed black screen flashing whenever something spawned (sv_forcepreload 0)
* Fixed a bug where the save/restore system of AngelScript would be broken if you recompiled the scripts on the fly
* AngelScript: Fixed memory leaks
+ Added `ImAThrowableWeapon` P3S attribute
+ Added `ImASprayWeapon` P3S attribute
+ Added `ImAMeleeWeapon` P3S attribute
+ Added `ImAMetaWeapon` P3S attribute
+ Added `extend` P3S section, behaves like `behavior` but it can only add new states or events for an already existing behavior
* Fixed `OnTargetKilled` p3s event not working properly for NPCs
+ Added `OnTargetKilled` p3s event to work with Player death
* Active weapon no longer gets holstered when jumping
* You can now fire with any weapon while jumping/in air
* You can now kick while jumping/in air
+ Made the HUD's color customizable via scripting
+ Ultrapatch Angel is now compatible with all copies of Postal 3
* Fixed Player camera in Demo playback not working
+ Added Colt
+ Added Autoshotgun
+ Added M249
+ Added Tear Gas
* Fixed a crash when looking at an entity while they were being removed from the game
* Fixed a crash when running into lots of physics props (Could be replicated in shops)
+ Shovel can now get bloody like the Machete
+ Added `ea_transition` Postal3Script attribute, forces entities to travel with the Player(s) even without a single trigger_transition present
+ Added `OnInsideTransition` and `OnOutsideTransition` Postal3Script events, need `ea_transition` attribute for them to work
+ Added `ea_NoTargetSenses` for NPCs, when set the NPCs will no longer set the FSM target automatically when choosing a target for seeing, instead it will be set as the FSM Caller  
this will not work as is and will require slightly rewriting the NPCs AI (the one Akella/TM wrote)

+ AngelScript: Added `bool CP3SObj::IsDeleted()`, checks if a Postal3Script object is null (internally)
+ AngelScript: Added `bool CP3SObj::IsValid()`, calls `IsDeleted()` and checks if the object has been marked as destroyed (acts as a combination of `bool CBaseEntity::IsDeleted()` and `bool CP3SObj::IsDeleted()`)
* AngelScript: Fixed serialization (saving script state to save file) being utterly broken
* AngelScript: Fixed EHANDLE object crashing the game if game has been saved
* Fixed several Addon System crashes/issues
+ Added FPS camera, use `p3_ultrapatch_cam_fps_toggle` to toggle between TPS and FPS
- PhysX is no longer functional, and thus has been removed (There are no plans bringing it back)
+ Replaced the Main Menu with a custom implementation, the TV main menu (map main_menu) is unaffected
+ Added `-novid` startup parameter, completely skips intro videos playing on main menu
+ Intro/credits videos can now be skipped
* `p3_cam_shake` will no longer disable quake effects
+ AngelScript: Added `[HIDDEN]` metadata, allows certain methods or classes to be completely hidden from console autocompletion
+ AngelScript: Added `[EXECUTABLE]` metadata, allows methods to be executable even if cheats is not on
+ Added Discord RPC, shows current map, current mission, current karma, and so on...
+ AngelScript: Made `CTakeDamageInfo` a proper value type, rather than a pointer/ref type
+ AngelScript: Hurt function hooks now require `(const CTakeDamageInfo& <name>)` to work
+ AngelScript: Hurt functions can now override the Damage Info by simply having the function *return* the variable  
they will still call into hardcoded damage functions, they'll soon be scriptified
+ AngelScript: Added console command `wj_angelscript_exec_owner`, same as `wj_angelscript_exec` but the console executioner will be the script owner
+ AngelScript: The following entities now also have base entity functions: `CParticleSystem`, `CP3Weapon`, `CWJProp`, `CP3Player` - eliminating the need to manually cast
* AngelScript: Optimized a little bit the class+function execution code
+ Added `-disablevideo` param to completely disable playing videos (intro, cutscenes, etc.), like during demos
* Fixed camera entities being broken like for example `func_monitor`
+ AngelScript: Added `SEngine OnEntitySpawn` hook, calls before the entity is being spawned (giving a chance to modify/or even delete entities/or etc.)
* Fixed Ultrapatch Angel not working on non-Windows operating systems (like Linux or other Unix OSes, making it compatible with devices like Steam Deck)
* Fixed P3S Weapon Base pointer pointing to the P3S entity itself, and not the weapon it was attached to
+ AngelScript: Added Global function atoi
+ AngelScript: Added Global function atof
+ AngelScript: Added Vector2D
* Fixed cutscene camera "blinking" whenever active camera was disabled
* AngelScript+Internal: Fixed being able to detach weapons from the Player like emptyhands, weemote, and matches
* AngelScript+Internal: Fixed active weapon glued to the Player when detached
* AngelScript: Fixed CP3Player::GiveAmmo using wrong function to call causing it to always crash the VM
+ AngelScript: Exposed most of CBaseCombatCharacter functions inside CBaseCombatCharacter@
+ AngelScript: Exposed most of CBaseCombatWeapon functions inside CBaseCombatWeapon@
+ AngelScript: Exposed most of CBasePlayer functions inside CBasePlayer@
* AngelScript+Internal: Fixed dropped weapons being invisible
* Fixed game crashing due to P3S pointer dingling around while destroyed and trying to access it (Affected NPCs only)
+ AngelScript: Added internal docs for classes, functions and methods
* AngelScript: Upgraded library from 2.35.1 to 2.38.0
* AngelScript: Fixed cache bugs, and improved efficiency
+ Added console command `p3_ultrapatch_ik`, controls whether IK is enabled for Players and NPCs or not. Helps with Multiplayer a bit. (by default it is 1)
+ Restored some functionality of the cut Inventory system (use, drop; partially ported from Catharsis Reborn)
+ Added automatic inventory item creation system (for addons: `p3/addons/<name>/scripts/<name>_inventory.txt`  
look inside `p3/scripts/ultrapatch/p3_ultrapatch_inventory.txt` for example  
* AngelScript: All console errors now start after AngelScript has (un)successfully compiled
* AngelScript: global function `GetCrosshair` now returns `CBaseEntity@` rather than `CP3SObj@`
* Fixed destroyed/removed ragdolls corrupting the game, eventually leading to awful crashes
+ Added scriptified flames (When NPCs are on fire) (B:bh_flame; M:Flame; F:Effects; I:st_init; S:st_start)
+ Added scriptified sounds (Sound hints from HL2) (B:bh_sound; M:Sound; F:Sound; I:st_init; S:st_start)
* Removed shoot restriction when covering, you can now shoot at any angle while in cover mode
* AngelScript: Fixed `CP3SObj` and `CBaseEntity` reporting as not null when they are released in memory
* Fixed crash involving ragdolled alive NPCs and explosions/or other types of damage
* Fixed crash involving NPCs trying to open Doors in their pathfinding (HL2 AI leftover, it was never working properly)
+ NPCs can now open Doors just by simply touching them, eliminating the need for trigger brushes
+ Players can now open Doors just by simply touching them, eliminating the need for trigger brushes
+ AngelScript: Added `GetRagdollPtr()` getter for `CP3SObj`
+ AngelScript: Added `DoRagdollBoogie` function for `CPhysObj`
* Postal3Script: `OnTargetKilled` and `OnTargetUnconscious` will now send events to all Postal3Script entities (including non-NPC/Player ones)
+ Added in-game Addon manager
+ Added engine hack made by Grizzle (increased engine model and sound precache limits)
* Fixed all props trying to transfer over via `changelevel` or triggers in Multiplayer 
* Slightly altered the code of pathfinding
* TVCam weapon (NPC-only weapon) is no longer restricted to Uwe Boll mission only
* Postal3Script: Fixed `Weapon pickup` not working properly
- Added `DropActiveWeapon` console command
* Fixed Monkey & Champ echo sounds
* AngelScript: Fixed active contexts being attempted to be used for execution
* Fixed player ragdolls going through player clips
* Fixed flashbang (unused projectile) deafening the Player forever
+ Added skill-based damage scaling for the Player (Easy, Normal, Hard like in HL2)
+ Added fire interval damage (Similar to POSTAL2)
* Dropping weapons no longer removes the ammo type of that weapon anymore
+ Added L4D/Alien Swarm instructor system
* Fixed some weapons still ticking while game is paused
+ AngelScript: Custom cvars with `FCVAR_ARCHIVE` now remember their values (stored inside `cfg/config_angel.cfg`
+ Added `env_hudhint` from Catharsis Reborn
* Fixed the scoreboard and spectator UI looking like an ugly mess
* Fixed hockey moms having a stare contest with Ron
* Fixed Segway crashing the game when it was trying to modify its own sound while ridden (it was most sensitive while transitioning)
* Fixed Segway (ridden) not transferring with the Player on transition
* Fixed AngelScript crash when trying to restore custom ConVars
+ Added `p3_ultrapatch_spawn_segway` cheat console command
+ Added `p3_ultrapatch_ai_can_climb` cheat cvar, controls whether Human NPCs can climb or not
+ Added `ea_invulnerable` P3S attribute, which makes NPCs invulnerable to any damage or interaction
+ Added scripted anim events for Human NPCs: `KICK` and `PUSH_HEAVY`, models need to be modified if you want to use them
* Human NPCs when performing attacks with melee weapons will now always grab damage values from active weapon, previously this was hardcoded to use their melee weapon pointer which is not always correct and could cause issues
+ Human NPCs with melee weapons when attacking now properly play melee hit sounds based on weapon sound (if this is gone wrong for some reason it fallbacks to `P3_NPC_Military.WeaponBash`)
* Human NPCs now perform Ranged or Melee attacks based on IsRanged and IsMelee weapon script flags from active weapon (previously this was also hardcoded to ranged or melee weapon pointers)
* Fixed rare crash when Human NPCs trying to pick up non-existent FSM prop/item
* Cat Dervish period increased from 12 seconds to 24 seconds
* Cat Dervish target pathing now takes FSM/P3S targets as first priority, previously it would only target enemies first (which would randomly disappear....) and then after FSM targets
+ Thrown Cats are now marked with `ThrownByDude_Init` P3S attribute
+ Thrown Cats with Grenades inside them now have `GrenadeAttached` P3S attribute
+ `ea_catbite` on Cats will now enable "Headcrab behavior"
+ [ea_catbite] `OnBite` event is fired when Cat pounced on something, `OnBitten` is fired inside the victim
* Thrown Cats will now immediately start targeting nearby NPCs
+ Added `ea_dervish`, returns 1 when Cat's state is in Dervish mode (it shouldn't be modified!)
* `p3_plr_snatch_bitedmg` from 5 to 3
* `p3_dog_bitedmg` from 5 to 10
+ Added jump attack (without snatching aka. headcrab attack) for Animals
* Fixed Dogs bugging out when trying to kick them (any damage that causes ragdoll) while they are in the air/trying to snatch
+ Dogs when trying to use Snatch function on non-Humans or the player they will instead damage the target upon touch (headcrab behavior), rather than just doing nothing
* Fixed Dogs sometimes getting stuck pouncing the air while in non-combat state
+ Added `ea_nopushing` attribute for Citizens, prevents being pushed around while idling by the Player
* Citizens while in Alert or Combat state group are no longer possible to be pushed around
* Citizens if their enemy is the Player can no longer be pushed around by the Player anymore
* Motorhead will now stick to where the laser is actually
* Fixed a crash when Motorhead tries to pick up non-existent items (could this be why one map crashed so much?)
* Fixed a crash when Motorhead tries to throw non-existent items
+ Added Motorhead Gear sound via anim event (`Motorhead.HeadMotor`=`AE_MOTORHEAD_MOTOR_GEAR`)
+ Added Monkey footstep sounds via anim event (`Monkey.Step`=`AE_MONKEY_FOOTSTEP_LEFT`/`AE_MONKEY_FOOTSTEP_RIGHT`)
* Helicopter will now ALWAYS use its P3S target for targeting and shooting
+ Added `OnTargetKilled` P3S event for the Heli
* Heli can now start smoking and it will intensify the less health it has (feature from CR)
* Fixed out of bounds memory access inside throwable weapons when trajectory is shown
+ Added valid index checks inside ballistic trajectory system, hopefully we won't hear about grenade crashes anymore
+ Added null checks for NPC speech
+ Added Imgui, `INSERT` to bring it up and `END` to force it to run in the background
+ Players now store a P3S attribute called `map_<mapname>` which tells if a Player has visited a specific map before or not
* Attempt fix for Karma & Transition combo, the way karma is handled might need a rewrite
+ Added `PlayerThrower` P3S attribute for cats, default is 0, if higher than 0 then it was thrown by a player
* Fixed being able to add multiple entries of the same type of a Mission log objective
+ Added `Object:weapon` P3S pointer, gets the pointer to active/held weapon of NPCs and Players
+ Added `Object:inventory` P3S pointer
+ Added `Object:myowner` P3S pointer (called inside held entities like items or weapons)
+ Added `Object:attacker` P3S pointer, only works for Players and NPCs
* Fixed `Ignore` category type not working (https://whackjobint.github.io/postal3script/objects/ignore/)
+ Extended P3S function Follow with 9 new params (simple, wide, antlion, commander, tight, medium, sidekick, hunter, vortigaunt)
+ It is now possible to use `Item throw` on corpse items
+ Added `p3_ultrapatch_p3s_changeattr_bugfix` cvar, default is 0, creates non-existent attributes first, then checks its value
+ Added `p3_ultrapatch_p3s_ifattr_bugfix` cvar, default is 0, creates non-existent attributes first, then checks its value
* Fixed Item drop absolutely not working on NPCs
+ You can now hide or unhide Mission log objectives (inside P3S)
+ Added about 41+ new P3S functions from CR, you can find some examples from here: https://github.com/WhackJobInt/postal3scriptdocs/commit/8e79d29f859cdc46f4a183a7b14e5f9611a2d545
+ Mission Log entity can now be forced to travel with the Player (controllable via `p3_ultrapatch_mission_log_transition`, default is 0, remembers the value, NOT RECOMMENDED for Vanilla)
* Fixed gibbing damage to the ragdolls (basically any kind of explosion near alive Human NPCs) causing a crash
+ Added `weapon_cubemap` that was missing for some reason, this weapon also has an alternative clientclass name `p3_weapon_cubemap` for consistency sake with P3 weapons
* Fixed game crashing when trying to use AngelScript console commands while it was disabled
* Fixed game crashing when trying to save/load while AngelScript was disabled
+ Added `p3_func_nav_areagroup`
With this func entity which manipulates navmesh you can:
- Set a custom area group for the affected area
- Whether affected areas should be area prohibited initially or not
- Toggle prohibit area

+ Added `string CP3SObj::GetCurAreaGroup()`, gets last known area group from Players, NPCs, and FSM Prop items (owners/equipped only)
+ Added `string CP3SObj::GetCusCurAreaGroup()`, it'll default to `AG_CUS_NONE` if there is no custom area group set for current area
+ Added `p3_ultrapatch_nav_dump_custom` console command
+ Added `p3_ultrapatch_nav_areagroup_debug` console command
* Mission log now removes all objectives before proceeding to the next mission (`EndMission` P3S func only)
- Unlinked p3_prop_physx
* Various null checks for Tank
* Fixed weapons having no P3S pointer at all on restore which causes memory leaks all over the place
* Fixed Valve's ragdoll cleanup system still breathing oxygen
+ Achievements no longer start triggering when starting from `pdb` or `pw`, they will always be active on any map as long as you don't have cheats turned on
* Missions will now remove past entities to combat save bloat, this might or might not break some things if Player is able to go back to a previous mission area (`EndMission` p3s function only)
* Fixed Weapon P3S pointers not traveling with the Player's weapons, thus forgetting values/memory after transition
* Much more aggressive Postal3Script pointer checks, this makes the game more stable and lessens the chance to crash (game still crashes due to outside factors beyond our control i.e. `materialsystem`, `studiorender`, etc..)
+ Added a way to manipulate the main menu TV via gameui2 (check out `resource2/mainmenu.res`)
* The game now prevents alive ragdolls to be deleted if it's unsafe (Held, used by an NPC when ragdolled, unconscious, etc...)
* Fixed OnTargetReset not calling for entities that had the deleted P3S entity as their target
* Fixed weapons (dropped and held by NPCs) not fading out in the same distance as NPCs
* Fixed other item types (FSM prop, Segway, cars, etc..) not fading out in the same distance as NPCs
+ Added `p3_ultrapatch_cleanup_weapon_threshold` which controls the maximum amount of dropped weapons, older ones always get deleted first
+ Added `p3_ultrapatch_cleanup_ragdoll_threshold` which controls the maximum amount of corpses laying around, older ones always get deleted first
* Fixed weapon and corpse P3S pointers lingering around which caused entity "leaks" and lags
* `p3_cam_shake` is now an archived convar
* Fixed all instances of dropping weapons (Players & NPCs) not properly setting `active` and `dropped` attributes (both of them should be 1)
* Fixed `notarget` cheat not working with Postal3Script
* Fixed Player holding limbs/props weirdly after save/load
* AngelScript: Fixed `QAngle` missing `x` variable
+ AngelScript: Added `Vector CP3Player::GetP3CameraOrigin()`
+ AngelScript: Added `QAngle CP3Player::GetP3CameraAngles()`
* AngelScript: `gpGlobals.eLoadType` is now a variable type (previously it was a function)
+ AngelScript: Added `MapLoadType_t` enum (MapLoad_Transition, MapLoad_Background, etc...)
* Fixed AngelScript crashing the game on save with empty `CBaseEntity@` (and derived from) pointers
+ Added `p3_ultrapatch_continuegame_auto`, if it's on Continue game also considers auto saves when loading latest saved game
+ Added `p3_ultrapatch_continuegame_quick`, if it's on Continue game also considers quick saves when loading latest saved game
* Fixed game crashing when NPC tries to throw invalid/corrupted item
+ Added `mapreplace` feature (made by Grizzle, and modified a little bit, check example file to see how it works)
* Completely removed restrictions where you couldn't shoot or jump while attacking
* Reduced the wait time before next possible kicking
* Kicking no longer caps the maximum speed to Walking speed
* Jumping animation now uses `ACT_DEAGLE_JUMP_AIM` for all weapons instead of the beehive jump one which was a lot more awkward
* Added safeguards for SetPlayerSkin function when skin name is NULL to prevent crashes, it will now revert to `coat` skin by default
+ Added `p3_ultrapatch_skin_mission`, enforces Skin from map onto Players on spawn or restore (enabled by default)
+ Added `p3_ultrapatch_skin_restore`, attempts to restore Skin when on a transition or save/load (enabled by default), should be disabled for open world mods that handle their own skin usage
* Fixed Use Target finder not finding nearby useable entities near crosshair's end trace
   `func_` entities will now properly glow if they have a prop attached to them as parent
* Fixed entities with P3S pointers always glowing if they don't have scripting (default_behavior) attached to them
* Fixed area portal bugs with Doors
* Fixed Doors that should not move at all on first interaction (either via touch or input functions) would move
* Increased Player ragdoll cooldown from 1 to 3 seconds (this should give plenty of time to get unstuck)
+ `dumpentityfactories` now highlight overridden classes
+ Added `-nooverride` which disables certain Angel class overrides (ragdoll, area portal, doors, etc.. check `dumpentityfactories` for more) only meant for debugging broken Angel features
* Fixed being able to kick while being dead
* Rocket no longer pops in and out of existence in the distance
* Fade distance values are now 100% consistent between all 7 types (NPCs, Boltons, Cars/P3 Animated props, P3 FSM props, Weapons, Ragdolls, Segways)
* Increased maximum active sound slots from 128 to 512, fixes AllocSound errors on mtz and possibly on other maps as well
+ AngelScript: Added hook `SEngine OnEntityCreated`
+ AngelScript: Added hook `SEngine OnEntityDeleted`


MULTIPLAYER RELATED:
====================
v1.3.0 (Multiplayer changes):

* Fixed various crashes
* Respawn lines are no longer being spammed
+ Added 8 new teams the mappers/scripters can now choose from (only in Teamplay)
(Dude, Agent, Cop, Security, Prisoner, SWAT, Krotchy, Janitor, Patrol)
+ Added proper team handling for the scoreboard
+ Added spectator mode
(spectator menu is tied to cover button)
* Bots will now properly face enemies or players
* Bots will no longer attempt going inside players
* Bots can now automatically respawn on their own
* Players joining a server will now force them to be in spectator mode by default
* Fixed Scoreboard not updating its values instantly until you toggled it on or off 2 times
+ Added `p3_ultrapatch_mp_autobalance` convar, by default it is on
+ Added `p3_ultrapatch_mp_deathmatch` convar, by default it is off
+ Added `p3_ultrapatch_mp_teamplay` convar, by default it is off
+ Added `p3_ultrapatch_mp_coop` convar, by default it is on
+ Added `p3_ultrapatch_mp_coll` convar, disables Player collision between other Players, it is off by default in coop, otherwise on
* Fixed covering not working
* "Fixed" black textures issue (band aid solution)
+ Added limited Dedicated server support, it is still recommended to host in-game
* Fixed disconnected or kicked players/bots leaving behind ragdolls and flying boltons
* Fixed newly connected Players screwing up storyline controller's instance
* Fixed bots crashing the game when respawned because they were far away in the void
* ""Fixed"" UTIL_GetLocalPlayer assert errors
- Players can no longer manually respawn 

