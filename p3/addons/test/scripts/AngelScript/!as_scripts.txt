// Player
class Playera : IPostal3Script
{
	CP3SObj@ self;
	Playera(CP3SObj@ obj)
	{
		if (@obj != null)
			@self = @obj;
		else
			@self = GetPlayer();
	}
	
	void KeyValuesTest()
	{
		KeyValues@ pData = CreateKeyValues("AddonList");
		pData.LoadFromFile("scripts/addonlist.txt");

		int i = 1;
		for (KeyValues@ pKV = pData.GetFirstSubKey(); pKV != null; @pKV = pKV.GetNextKey())
		{
			//Printf("Addon %d: '%s' (%d)\n", i, pKV.GetName(), pKV.GetInt(pKV.GetName()));
		}
		
		pData.deleteThis();
	}
}